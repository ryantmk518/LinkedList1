#include <iostream>
#include <cstring>
#include <String.h>

//Node Header

using namespace std;

class Node{
public:
  Node(Student*);
  ~Node();
  void setValue(int newvalue);
  int getValue();
  void setNext(Node* newnext);
  Node* getNext();
private:
  int value;
  Node* next;
};
